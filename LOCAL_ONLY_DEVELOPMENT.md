# Local-only development

This working tree — on branch `signal-state-local-build` — is for **local Signal & State / Ground Control commercial development only.**

1. This working tree is for local Signal & State development.
2. No commits are authorized yet.
3. No pushes are authorized yet.
4. No deployments are authorized yet.
5. The current Vercel project is reserved for the portfolio version (commit `f51b264721d5b16272a2d07cb90032ad247356cf` on `main`, already deployed).
6. The future commercial application must use a separate Vercel project.
7. The future commercial application should use a separate production database.
8. The future commercial application should use separate environment variables.
9. The future commercial application should use a separate domain or subdomain.
10. Any deployment instruction written anywhere in this repository (including `FUTURE_DEPLOYMENT_HANDOFF.md`, `PRODUCTION_MIGRATION_PLAN.md`, or any script) is **documentation only** and must not be executed without separate, explicit, future authorization.

See `PORTFOLIO_PRESERVATION.md` for the preservation record and `../ground-control-local-checkpoints/` for out-of-repo recovery checkpoints.
